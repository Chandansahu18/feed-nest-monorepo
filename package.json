{
  "name": "feednest-monorepo",
  "private": true,
  "workspaces": [
    "client",
    "server",
    "types"
  ],
  "scripts": {
    "build": "npm run build --workspace=types && npm run build --workspace=server && npm run build --workspace=client"
  }
}
